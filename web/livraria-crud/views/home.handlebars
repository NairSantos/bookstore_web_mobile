<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Livros</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container">
        <h1 class="mt-4">Livros</h1>
        <a href="/livro/create" class="btn btn-primary mb-3">Adicionar Livro</a>
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Título</th>
                    <th>Autor</th>
                    <th>Ano</th>
                    <th>Ações</th>
                </tr>
            </thead>
            <tbody>
                {{#if livros.length}}
                {{#each livros}}
                <tr>
                    <td>{{this.titulo}}</td>
                    <td>{{this.autor}}</td>
                    <td>{{this.ano}}</td>
                    <td>
                        <a href="/livro/edit/{{this.id}}" class="btn btn-warning btn-sm">Editar</a>
                        <a href="/livro/deletar/{{this.id}}" class="btn btn-danger btn-sm">Deletar</a>
                    </td>
                </tr>
                {{/each}}
                {{else}}
                <tr>
                    <td colspan="4" class="text-center">Nenhum livro encontrado.</td>
                </tr>
                {{/if}}
            </tbody>
        </table>
    </div>
</body>
</html>
